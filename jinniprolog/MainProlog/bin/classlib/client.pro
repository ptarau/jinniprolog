:-[rli_client].

client:-rli_client.

client(P):-rli_client(P).

client(H,P):-rli_client(H,P).

client(H,P,W):-rli_client(H,P,W).


/*
:-[keep_alive_client].

client:-keep_alive_client.

client(P):-keep_alive_client(P).

client(H,P):-keep_alive_client(H,P).

client(H,P,W):-keep_alive_client(H,P,W).
*/

%@@