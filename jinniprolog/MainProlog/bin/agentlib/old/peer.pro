/*
  P2P Agent Class. Starts a registry service automatically on localhost 
  if such a service does not exist and then registers. Looks for other P2P
  agents at well-known servers/ports.
*/

:-[gui_agent].

%@@